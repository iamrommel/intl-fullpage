import React from 'react'
import { PageSection } from './components/PageSection'

export const CupSizeSection = () => {
    return (
        <PageSection id={'cupsize'} className={`bg-cupsize`}>
            <p>Select A cup</p>
        </PageSection>
    )
}

